This repo contains implementation of optimizations of two-stream instability.

The following are the different optimization techniques that can be found:
  1)Cython Optimization
  2)GPU acceleration with PyTorch
  3)Parallelization of Cython optimised code with DASK
  
This repo also contains pure python implementations.
